import React from 'react';
export type FileDownloadButtonProps = {
    content: string;
    fileName: string;
    fileType: string;
};
export declare const FileDownloadButton: (props: FileDownloadButtonProps) => React.JSX.Element;
